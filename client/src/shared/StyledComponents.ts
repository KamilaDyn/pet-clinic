import { styled } from "styled-components";

export const FlexBox = styled.main({
  display: "flex",
  justifyContent: "space-around",
  alignItems: "center",
  flexWrap: "wrap",
  rowGap: "3rem",
});
